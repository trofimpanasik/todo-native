import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#282C34',
        paddingTop: 100,
        alignItems: 'center'
    }
});