import { StyleSheet } from "react-native";

export default StyleSheet.create({
    text: {
        color: '#fff',
        fontSize: 20,
        marginTop: 10,
        marginRight: 'auto',
        marginLeft: 'auto'
    },
})