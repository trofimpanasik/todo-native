import { StyleSheet } from "react-native";

export default StyleSheet.create({
    Container: {
        marginTop: 10,
        marginBottom: 10
    },
    Text: {
        fontSize: 30,
        color: 'white',
        fontWeight: '700',
    }
})